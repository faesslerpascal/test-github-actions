name: convert md
on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  convert-md:
    runs-on: [ "ubuntu-latest" ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: show checked out content and disk free
      run: |
        ls -l
        df -h
